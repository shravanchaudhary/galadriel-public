# Conversation buffers

The runtime may create temporary per-channel recovery buffers here. A fresh Replika
starts with no conversation history. Do not seed or copy one tenant's buffer into
another tenant.
