# Browser profiles

This registry maps a browser `profile_id` to the connection details required by the
configured browser backend. Read it before selecting a browser profile.

Never store browser passwords, cookies, API tokens, or other secrets here. Add or
update a row only when the user pairs or renames a browser profile.

| profile_id | pairing_code | purpose |
|---|---|---|
| None configured | — | — |
