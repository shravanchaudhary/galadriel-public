# Plan — 2026-07-01

- **Due Rituals (Missed morning run, running now):**
  - Check and process LinkedIn Invitations.
  - Autonomous Sales Engine (strict 10/day limit for bare connection requests).
- **Steering Corrections Folded In:**
  - **(New: 20:35 CET):** STRICT EXCLUSION of "Software Engineer" / "Developer" roles. The worker must meticulously verify current titles before queuing leads to avoid polluting the ICP (Founders/VCs).
  - When moving leads to approval states (e.g., `review_pending`), must run `db_update(fields={"draft": "..."})` BEFORE `db_move_state` to ensure text is visible in the UI.
  - Inside loops, MUST check `db_counter` & `at_cap` BEFORE EVERY SINGLE send, and increment immediately AFTER. Do not tally at the end.
- **Carried Forward:**
  - Leads in `review_pending` awaiting Shravan's approval for post-acceptance chit-chat openers.
