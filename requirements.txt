anthropic>=0.52.0
google-genai>=2.0.0
ollama>=0.4.0
discord.py>=2.5.0
# Slack gateway (alternative to Discord) — Socket Mode needs aiohttp
slack-bolt>=1.20.0
aiohttp>=3.9.0
Flask>=3.0.0
waitress>=3.0.0
python-dotenv>=1.0.0
boto3>=1.43.41
mempalace>=3.3.2,<3.4
# HTTP client (fetch_url_data, google_search)
httpx>=0.27.0
tenacity>=8.0.0
pydantic>=2.0.0
# TOTP generation for LinkedIn two-factor login
pyotp>=2.9.0
# Async MongoDB driver for automation scripts (AsyncMongoClient; motor is EOL)
pymongo[aws]>=4.9
# External Valkey cache/readiness connection; no local Redis container.
redis>=5.0.0
# Fuzzy company/prospect name matching for the Explorium client cache
thefuzz>=0.22.0
# Screenshot downscale for vision attachments (browser tool)
Pillow>=10.0.0
# Local prompt/context compression. The default pipeline is CPU/text-only;
# ML extras pull multi-gigabyte CUDA/PyTorch wheels that Fargate cannot use.
headroom-ai>=0.30.0
