- **Preserve raw voice:** Never polish Shravan's raw edits into standard AI copy. Keep user-provided wording intact and close posts simply.
- **Track draft revisions strictly:** Always present the newest revision; never regress to an earlier draft.
- **Rate limits apply inside loops:** Check and increment `db_counter` before every individual external action.
- **Save drafts before approval:** Persist drafted text with `db_update` before moving an entity to `review_pending`.
- **No idle ledger entries:** Write to `state/progress/` only for completed actions, DB transitions, or real blockers.
- **Execute overdue plan checkboxes:** The worker reads today's plan every tick and immediately handles unchecked rituals whose time has passed.
- **Never guess LinkedIn URLs:** Use the exact URL returned by Explorium or extracted from the live browser.
- **No hyper-personalization:** Avoid contrived profile/post references that reveal automation; use simple, direct language.
- **Outbound caution:** If qualification or context is uncertain, wait instead of forcing low-confidence outreach.
- **Clean outbound formatting:** No bot-like emoji and no wrapping quotation marks around messages.
- **Manage review backpressure:** When approval queues are saturated, stop creating drafts and prioritize review resolution.
- **Use native text insertion for React editors:** Focus the target and use `document.execCommand('insertText', false, text)` when normal typing corrupts component state.
- **Keep browser sessions alive:** Never close the last tab; release ownership in `state/browser_tabs.md` instead.
- **Recover from BCE `TAB_INVALID`:** Navigate the active tab to a valid web page; extension source changes require a browser/extension restart.
- **No interrogation-style openers:** Start with low-friction peer-level notes, not heavy questions about priorities or focus.
- **Filter inbound noise:** Do not promote known contacts, visibility-seeking engagement, emoji-only comments, or generic flattery into the sales pipeline.
- **Voice sourcing:** Learn personal voice from the user's own messages; only fall back to organization-level patterns when personal history is unavailable.
- **Gemini nested arguments:** Recursively convert protobuf Struct values to native dictionaries before passing nested filters to PyMongo.
