- **Preserve raw voice:** Never "polish" Shravan's raw edits into standard AI copy. If he provides a draft or an edit, keep it exactly as it is. He actively rejects text that sounds "straight outta ai". Close posts simply and minimally.
- **Track draft revisions strictly:** When iterating on a post, always present the absolute newest revision. Do not accidentally regress to earlier versions in the chat.
- **Rate limits apply inside loops:** When executing bulk actions (like sending connection requests), check and increment `db_counter` *before every single action*, not at the end of the batch. Never bulk-execute without a per-item cap check.
- **Save drafts to DB before seeking approval:** When moving an entity into an approval state (like `review_pending`), you must use `db_update` to save the drafted text to the DB document *before* using `db_move_state`. Approval states are read from the DB by the UI; text recorded only in the progress file is invisible for review.
- **No idle logging in the shared ledger:** Do not append an entry to today's progress file (`state/progress/`) if a background check yields no changes. The ledger is for completed actions, DB state transitions, and real blockers, not heartbeat ticks.
- **Worker Checkbox Execution:** The worker must read today's plan file (`state/plan/<today>.md`) on every tick. If a scheduled ritual's timestamp has passed and it is unchecked, the worker must execute it immediately to prevent missing tasks.
- **NEVER construct or guess LinkedIn URLs:** Never assume a prospect's URL is `linkedin.com/in/first-last`. Prospects frequently customize vanity URLs, resulting in 404 errors down the pipeline. You must ALWAYS extract the exact URL returned by the Explorium API or directly evaluate the `href` attribute from a live browser search (`eval "document.querySelector(...).href"`). Guessing URLs is strictly prohibited.
- **NO hyper-personalization / AI-agent giveaways:** Do not perform hyper-personalization by scraping prospect profiles or posts (e.g. "I enjoyed your post on X" or "I saw your breakdown on Y"). The more we hyper-personalize, the more visible it is as AI/automation, which throws off prospects. Reach out simply and directly without contrived hooks. We must appear as an AI platform automating on the user's behalf, not as an AI agent trying to act like a fake human.
- **Outbound Caution (The Long Game):** Do not generate outbound if you are not sure. Let it rest. It's a long-game strategy. If you lack clear context, positive qualification evidence, or exact user alignment, wait instead of forcing a low-confidence action. We prefer high-confidence patience over high-volume guesswork.
- **Avoid technical and formatting errors:** Never use bot-like emojis/emoticons (e.g., 🤖, 🚀, 📈) in outreach. Never copy-paste wrapping double quotes (e.g., `"Haha, caught me! ... "`) in outbound messages—strip all wrapping quotes from generated text before pasting or sending.
- **Sourcing Voice Protocols:** 
  - Learn how a user talks by reading memory logs where every user message is logged.
  - When running organization-wide campaigns with multiple accounts, if an employee account lacks a distinct speaking history, leverage style patterns and messages from other employees of the same organization.
  - Analyze the company's official LinkedIn handle, their public posts, and their direct messages (DMs) to learn each employee's independent voice.
- **Injecting text into React Web Components (Shadow DOM):** Standard character-by-character keyboard simulations or setting `.value` on content-editable inputs can corrupt React's state management or get blocked by Shadow DOM wrappers. The most robust native solution is to use the browser-native `document.execCommand('insertText', false, text)` API after targeting and focusing the text container, which seamlessly updates React's internal state instantly without corruption.
- **Chromium Process Lifecycle on Tab Closure:** Closing the last active tab in a headless or headed browser session can trigger a lifecycle event that terminates the underlying Chromium browser daemon, losing persistent cookie stores and authenticated sessions. Maintain an inactive/idle tab pool (or navigate tabs to a neutral page like `about:blank` instead of aggressively closing them) to keep the daemon stably alive across background worker tasks.
- **Gemini SDK Protobuf Struct Conversion:** Gemini's SDK returning tool/function call args yields nested structured objects as protobuf Structs rather than native Python dictionaries. Since PyMongo expects standard dicts for sub-filters, queries with nested filters would silently return zero results. Ensure all function arguments containing nested schemas/filters are recursively converted from protobuf Structs to standard Python dicts before database execution.
