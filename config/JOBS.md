# config/JOBS.md — Goals and recurring work

This always-loaded index tells the Replika which recurring jobs are active. Detailed
steps belong in `jobs/<id>.md`; one-off projects belong in `state/backlog.md`.

## Broad goals

- No broad goals have been configured.

## Active rituals

| Ritual | Schedule | Cookbook |
|---|---|---|
| None configured | — | — |

## Maintaining this file

Add a ritual only after the user agrees on its goal, schedule, and cookbook. A missed
ritual runs at most once; it does not accumulate duplicate work. To pause background
work, set `state/worker_control.md` to `paused`.
