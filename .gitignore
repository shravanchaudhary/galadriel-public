.env
.galadriel-local/
.cursor/
infra/terraform/.terraform/
infra/terraform/*.tfstate*
infra/terraform/.replika-provisioner.zip
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
node_modules/
config/scheduler_state.json
config/ambient_state.json
memory/*.md
memory/*.json
!memory/README.md
*.log
.venv/
venv/
# Debug prompt dumps contain full system prompt content
debug/prompts/

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json
.vscode/
todo
state/conversation_buffers/*
!state/conversation_buffers/README.md
state/screenshots/
state/phone_bridge_auth.json

# Local untracked artifacts (see .cursor/rules/untracked-artifacts.mdc)
sessionmanager-bundle/
sessionmanager-bundle.zip
test_db.py

# Local GGUF weights (downloaded via `python -m local_llm download`)
local_llm/models/*.gguf
local_llm/models/*.partial
.ipynb_checkpoints/

# Eval benchmark suite: model download caches are disposable (the eval CLIs
# re-download on demand); eval/results/ stays tracked as benchmark evidence.
eval/models/*.gguf
eval/models/*.partial
